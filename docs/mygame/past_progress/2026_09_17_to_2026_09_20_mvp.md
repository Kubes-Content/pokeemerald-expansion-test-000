#### 2026-09-20: Mvp of an mvp of an mvp of an mvp (break it down until it's too small to break down)
- [x] minimal start
    - [x] force the player to have a random starter from game start
        - or a cat? or a llama???? he has a llama!
        - [x] test
    - [x] load a custom starting map (see: WarpToTruck)
        - [ ] why does quickstart play the truck animation if I change the map?
            - src.overworld.c:1944 = gFieldCallback = ExecuteTruckSequence;
    - [x] begin in an empty plot
- [x] refactor what we have to be less intrusive on source - more rebase/merge friendly (2026-09-20)
    - [x] rollback and reapply source changes w/ proper style
    - I know it's not fully possible, but separate game code where you can
        - and where you need it, keep the footprint small
    - an include and a macro where things need to be injected (this is more flexible than line(s))
        - no, macro for adding fields...
        - function calls where applicable
    - use custom \#ifdef statements to replace lines w/o removing them